import streamlit as st

st.title("AI Day Planner")

task = st.text_input("Enter a task")
duration = st.slider("Estimated duration (minutes)", 10, 180)

if st.button("Add Task"):
    st.write("Task added:", task)
